spring:
  profiles:
    active: development
  data:
    rest:
      base-path: /api

---

spring:
  profiles: development
  datasource:
    platform: postgresql
  jpa:
    show-sql: true
    generate-ddl: true
endpoints:
  hypermedia:
    enabled: true
  sensitive: false
debug: true

---

spring:
  profiles: production