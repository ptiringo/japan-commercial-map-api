spring:
  profiles:
    active: development
  data:
    rest:
      base-path: /api

---

spring:
  profiles: development
  datasource:
    platform: h2
  jpa:
    show-sql: true
debug: true

---

spring:
  profiles: production