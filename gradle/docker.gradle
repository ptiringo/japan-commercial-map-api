ext {
	dockerImageName = "${group}/${jar.baseName}:${jar.version}"
}

docker {
	name dockerImageName
	files jar, './docker/wait-for-it.sh'
	dependsOn bootJar
}

dockerRun {
	name "${group}_${jar.baseName}_${jar.version}"
	image dockerImageName
	ports '8080'
}